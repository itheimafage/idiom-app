export default definePageConfig({
  navigationBarTitleText: '成语列表',
  navigationBarBackgroundColor: '#D35400',
  navigationBarTextStyle: 'white',
})

function definePageConfig(config: any) {
  return config
}
